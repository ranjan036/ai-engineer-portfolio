✅ Use a Virtual Environment (Very Important)
python -m venv venv
to activate:
Windows:
venv\Scripts\activate
Mac/Linux:
source venv/bin/activate

✅ How to select a new venv:

after activating the venv, open the desired .py or ipynb file . 
click on select kernel , click python Environments, there it will show all venv. choose the one you need


✅ Install libraries inside it:

pip Install pandas numpy scikit-learn matplotlib ipykernel

🧠 ML PROJECT SUMMARY (DAY LEARNING)

1. Model Saving & Loading
   Train once → Save → Reuse later

- pickle.dump(model, file) → save model
- pickle.load(file) → load model

2. File Handling

- with open(file_path, "wb") as f:
- "w" = write, "b" = binary (required for ML models)

3. OS Module (Folders)

- os.path.dirname(file_path) → get folder path
- os.makedirs(folder, exist_ok=True) → create folder safely

4. CLI (Command Line Input)

- input() → take user input
- split(",") → parse input
- [float(x) for x in values] → convert to numbers

5. Error Handling

- try/except → prevent crash and handle invalid input

6. Argument Passing

- sys.argv[1] → dataset path
- sys.argv[2] → target column

7. Project Structure

- train.py → training + saving
- main.py → loading + prediction

8. Virtual Environment

- aiJourney\Scripts\activate → isolate dependencies

9. Streamlit (Web App)

- pip install streamlit
- streamlit run app.py

10. Streamlit UI Components

- st.title()
- st.write()
- st.number_input()
- st.button()
- st.success()
- st.spinner()

11. Layout

- st.columns(2) → split UI into columns

12. ML Pipeline Flow
    load_data()
    → prepare_data()
    → train_model()
    → evaluate_model()
    → save_model()
    → load_model()
    → predict()

13. Debugging Skills Learned

- fixed wrong file path
- fixed broken pandas install
- handled missing model loading
- corrected user input format

🚀 FINAL OUTCOME
✔ Built ML pipeline
✔ Created CLI interface
✔ Built Streamlit web app
✔ Debugged real issues
✔ Structured project properly